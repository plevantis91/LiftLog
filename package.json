{
  "name": "liftlog",
  "version": "1.0.0",
  "description": "A fitness web application for tracking strength training and intelligent recovery suggestions",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["fitness", "workout", "tracking", "recovery", "strength-training"],
  "author": "Peter LeVantis",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
